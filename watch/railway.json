{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "watch/Dockerfile",
    "watchPatterns": ["watch/**"]
  },
  "deploy": {
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
